// Prevent us from attempting to use variables
// that are not declared
"use strict";


/*
// 1. Create and log arrays to the console
let fruits = ["Apple", "Banana", "Cherry"];
let numbers = [1, 2, 3, 4, 5];
let mixedArray = ["Hello", 42, true, { key: "value" }]; // Array with mixed data types

console.log(fruits); // ["Apple", "Banana", "Cherry"]
console.log(numbers); // [1, 2, 3, 4, 5]
console.log(mixedArray); // ["Hello", 42, true, { key: "value" }]
*/

/*
// 2. An array with different data types and log it
console.log(typeof mixedArray); // "object" (Arrays are objects in JavaScript)
*/

/*
// 3. Remove items from arrays
fruits.pop(); // Removes "Cherry" from the end
numbers.shift(); // Removes "1" from the beginning
console.log(fruits); // ["Apple", "Banana"]
console.log(numbers); // [2, 3, 4, 5]
*/

/*
// 4. Add items to arrays
fruits.push("Grapes"); // Adds "Grapes" to the end
numbers.unshift(0); // Adds "0" to the beginning
console.log(fruits); // ["Apple", "Banana", "Grapes"]
console.log(numbers); // [0, 2, 3, 4, 5]
*/

/*
// 5. Change the value of an element in each array
fruits[1] = "Mango"; // Changes "Banana" to "Mango"
numbers[3] = 99; // Changes "4" to "99"
console.log(fruits); // ["Apple", "Mango", "Grapes"]
console.log(numbers); // [0, 2, 3, 99, 5]
*/